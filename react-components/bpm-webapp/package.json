{
  "name": "bpm-webapp",
  "private": true,
  "engines": {
    "node": ">=14",
    "pnpm": ">=8"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "start": "pnpm --filter bpmnjs run start",
    "build": "pnpm --filter bpmnjs run build"
  }
}
