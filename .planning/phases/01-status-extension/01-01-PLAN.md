---
phase: 01-status-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/domains/WkfInstance.xml
  - src/main/resources/views/Selects.xml
autonomous: true
requirements:
  - STAT-01
  - STAT-02

must_haves:
  truths:
    - "WkfInstance domain model declares STATUS_MIGRATION_IN_PROGRESS constant with value 4"
    - "The migration status selection widget includes a 'Migration in progress' option with value 4"
    - "After code generation, WkfInstanceRepository.STATUS_MIGRATION_IN_PROGRESS is available as a public static final int"
  artifacts:
    - path: "src/main/resources/domains/WkfInstance.xml"
      provides: "STATUS_MIGRATION_IN_PROGRESS = 4 constant in extra-code block"
      contains: "STATUS_MIGRATION_IN_PROGRESS = 4"
    - path: "src/main/resources/views/Selects.xml"
      provides: "Migration in progress option in wkf.instance.migration.status.select"
      contains: "Migration in progress"
  key_links:
    - from: "src/main/resources/domains/WkfInstance.xml"
      to: "build/src-gen/main/java/com/axelor/studio/db/repo/WkfInstanceRepository.java"
      via: "Axelor code generation (./gradlew generateCode)"
      pattern: "STATUS_MIGRATION_IN_PROGRESS = 4"
    - from: "src/main/resources/views/Selects.xml"
      to: "WkfInstance.xml migrationStatusSelect field"
      via: "selection='wkf.instance.migration.status.select' attribute"
      pattern: "option value=\"4\""
---

<objective>
Add the STATUS_MIGRATION_IN_PROGRESS (4) constant and its matching selection option to enable the intermediate migration state machine.

Purpose: Phase 2 needs this constant to mark instances as "in progress" between instance migration and task migration. Without it, there is no way to distinguish "instances migrated, tasks pending" from "fully done".
Output: Two XML edits -- one constant in WkfInstance.xml, one selection option in Selects.xml.
</objective>

<execution_context>
@/home/fbenbaich/.claude/get-shit-done/workflows/execute-plan.md
@/home/fbenbaich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-status-extension/01-CONTEXT.md
@.planning/phases/01-status-extension/01-RESEARCH.md

<interfaces>
<!-- Current state of both files the executor will modify -->

From src/main/resources/domains/WkfInstance.xml (lines 30-36, the extra-code block):
```xml
<extra-code><![CDATA[
    public static final int STATUS_NOT_MIGRATED = 1;
    public static final int STATUS_MIGRATED_SUCCESSFULLY = 2;
    public static final int STATUS_MIGRATION_ERROR = 3;
    public static final int STATUS_IN_PROGRESS = 1;
    public static final int STATUS_STOPPED = 2;
]]></extra-code>
```

From src/main/resources/views/Selects.xml (lines 33-37, the migration status selection):
```xml
<selection name="wkf.instance.migration.status.select">
  <option value="1">Not migrated</option>
  <option value="2">Migrated successfully</option>
  <option value="3">Migration error</option>
</selection>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add STATUS_MIGRATION_IN_PROGRESS constant and selection option</name>
  <files>src/main/resources/domains/WkfInstance.xml, src/main/resources/views/Selects.xml</files>
  <read_first>
    - src/main/resources/domains/WkfInstance.xml (verify current extra-code block structure at lines 30-36)
    - src/main/resources/views/Selects.xml (verify current selection block at lines 33-37)
  </read_first>
  <action>
**Edit 1 -- WkfInstance.xml (STAT-01):**

In the `<extra-code>` CDATA block, add the following line after `STATUS_MIGRATION_ERROR = 3` (line 33) and before `STATUS_IN_PROGRESS = 1` (line 34):

```java
    public static final int STATUS_MIGRATION_IN_PROGRESS = 4;
```

The extra-code block should look like this after the edit:
```xml
<extra-code><![CDATA[
    public static final int STATUS_NOT_MIGRATED = 1;
    public static final int STATUS_MIGRATED_SUCCESSFULLY = 2;
    public static final int STATUS_MIGRATION_ERROR = 3;
    public static final int STATUS_MIGRATION_IN_PROGRESS = 4;
    public static final int STATUS_IN_PROGRESS = 1;
    public static final int STATUS_STOPPED = 2;
]]></extra-code>
```

**Edit 2 -- Selects.xml (STAT-02):**

In the `<selection name="wkf.instance.migration.status.select">` block, add the following line after the `Migration error` option (line 36) and before the closing `</selection>` tag (line 37):

```xml
    <option value="4">Migration in progress</option>
```

The selection block should look like this after the edit:
```xml
<selection name="wkf.instance.migration.status.select">
  <option value="1">Not migrated</option>
  <option value="2">Migrated successfully</option>
  <option value="3">Migration error</option>
  <option value="4">Migration in progress</option>
</selection>
```

**Do NOT:**
- Change any existing constant values or labels
- Modify any Java files (Phase 2 concern)
- Edit generated files in `build/src-gen/`
  </action>
  <acceptance_criteria>
    - WkfInstance.xml contains `public static final int STATUS_MIGRATION_IN_PROGRESS = 4;`
    - WkfInstance.xml still contains all 3 original migration constants unchanged: `STATUS_NOT_MIGRATED = 1`, `STATUS_MIGRATED_SUCCESSFULLY = 2`, `STATUS_MIGRATION_ERROR = 3`
    - Selects.xml contains `<option value="4">Migration in progress</option>` inside `wkf.instance.migration.status.select`
    - Selects.xml still contains all 3 original options unchanged: value="1", value="2", value="3"
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'STATUS_MIGRATION_IN_PROGRESS = 4' src/main/resources/domains/WkfInstance.xml && grep -q 'value="4">Migration in progress' src/main/resources/views/Selects.xml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Both XML files contain the new constant and selection option with value 4, all existing values untouched</done>
</task>

<task type="auto">
  <name>Task 2: Verify code generation produces the new constant</name>
  <files>build/src-gen/main/java/com/axelor/studio/db/repo/WkfInstanceRepository.java</files>
  <read_first>
    - src/main/resources/domains/WkfInstance.xml (confirm the edit from Task 1 is present)
  </read_first>
  <action>
Run Axelor code generation to verify the new constant propagates to the generated Repository class:

```bash
./gradlew generateCode
```

After generation completes, verify that `build/src-gen/main/java/com/axelor/studio/db/repo/WkfInstanceRepository.java` contains:

```java
public static final int STATUS_MIGRATION_IN_PROGRESS = 4;
```

This confirms the Axelor code generation pipeline correctly reads the updated `<extra-code>` block from WkfInstance.xml and produces the constant that Phase 2 Java code will reference as `WkfInstanceRepository.STATUS_MIGRATION_IN_PROGRESS`.

If `./gradlew generateCode` is not available or fails, try `./gradlew build` from the project root directory (the parent of this module). The key is that the generated WkfInstanceRepository.java must contain the new constant.
  </action>
  <acceptance_criteria>
    - `./gradlew generateCode` (or `./gradlew build`) exits with code 0
    - `build/src-gen/main/java/com/axelor/studio/db/repo/WkfInstanceRepository.java` contains `STATUS_MIGRATION_IN_PROGRESS = 4`
    - The generated file still contains all 3 original migration constants: `STATUS_NOT_MIGRATED`, `STATUS_MIGRATED_SUCCESSFULLY`, `STATUS_MIGRATION_ERROR`
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'STATUS_MIGRATION_IN_PROGRESS = 4' build/src-gen/main/java/com/axelor/studio/db/repo/WkfInstanceRepository.java && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Code generation succeeds and WkfInstanceRepository.java contains STATUS_MIGRATION_IN_PROGRESS = 4</done>
</task>

</tasks>

<verification>
1. `grep 'STATUS_MIGRATION_IN_PROGRESS = 4' src/main/resources/domains/WkfInstance.xml` -- constant in source XML
2. `grep 'value="4">Migration in progress' src/main/resources/views/Selects.xml` -- selection option in views XML
3. `grep 'STATUS_MIGRATION_IN_PROGRESS = 4' build/src-gen/main/java/com/axelor/studio/db/repo/WkfInstanceRepository.java` -- constant in generated code
4. All existing constants and options unchanged (values 1, 2, 3 still present)
</verification>

<success_criteria>
- WkfInstance.xml has STATUS_MIGRATION_IN_PROGRESS = 4 in extra-code
- Selects.xml has "Migration in progress" option with value 4 in wkf.instance.migration.status.select
- Generated WkfInstanceRepository.java contains the new constant
- Build completes without errors
- All existing status constants and selection options remain unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-status-extension/01-01-SUMMARY.md`
</output>
