// Root barrel export for generic-builder
// External consumers (bpm, mapper) import from here instead of deep paths.
export { default as ExpressionBuilder, useDialog } from './src/views/index';
export { IconButton } from './src/components/index';
